# REBTB security evaluation simulator

* Run `python3 ./run.py` in each folder to save experimental data (if not already present) and generate figures. 
* Specifically, for the multi-target eviction scenario in the IBP, run `python3 ./eviction_muti.py` in the folder to save the corresponding experimental data (if not already present) and generate figures.